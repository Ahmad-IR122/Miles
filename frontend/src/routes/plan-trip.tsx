import TripPlanningForm from "../components/tripPlanningForm/tripPlanningForm";

const PlanTrip = () => {
  return (
    <section>
      <TripPlanningForm />
    </section>
  );
};

export default PlanTrip;
