# Project A — AI Trip Planner

An AI-powered travel platform that generates personalized itineraries, recommends destinations and activities, and lets travelers plan and manage trips through a conversational assistant.

## Structure

- `backend/` — FastAPI backend service. See `backend/README.md` for setup and run instructions.